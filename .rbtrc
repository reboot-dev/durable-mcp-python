# Find '.proto' files in 'api/'.

generate store/api/

# Tell `rbt` where to output its generated files.

# The paths are different for backend and frontend code.



# DELETE THIS
generate --python=store/api/
generate --react=store/web/api

dev run --python --application=main.py
dev run --watch=**/\*.py --watch=different**/\*.py
dev run --name=my-mcp
